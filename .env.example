# 개발·배포 모두 비워 둘 것. 코드의 '/api/v1/...'을 그대로 요청하면
# 개발 서버(vite.config.ts)와 배포(vercel.json)의 프록시가 백엔드로 넘긴다.
#
# 브라우저가 우리 도메인 하나만 상대하게 되므로
#   - 리프레시 토큰 쿠키가 퍼스트파티가 되어 사파리에서도 차단되지 않고
#   - CORS 허용 목록을 관리할 필요가 없다
#
# 여기에 백엔드 주소를 넣으면 브라우저가 백엔드로 직접 요청하게 되어
# 위 두 가지를 다시 신경 써야 한다
VITE_API_BASE_URL=
