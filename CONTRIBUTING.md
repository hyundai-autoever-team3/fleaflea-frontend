# 브랜치 전략

- `main`: 항상 배포 가능한 상태로 유지
- `feature`: 기능 단위로 생성 후 작업
- `release`: 배포 전 준비용

## 브랜치명 컨벤션

`feature/이슈번호-깃허브id`

예시: `feature/22-sor999`

## 머지 전략

일반 머지 (merge commit)

# 커밋 메시지 컨벤션

## 1. 커밋 유형 지정

커밋 유형은 영어 소문자로 작성한다.

- **feat**: 새로운 기능 추가
- **chore**: 코드 변경 없는 작업 (빌드, 설정 등)
- **design**: 디자인 관련 변경
- **fix**: 버그 수정
- **test**: 테스트 관련 작업
- **deploy**: 배포 관련 작업
- **refactor**: 코드 리팩토링
- **docs**: 문서 추가 및 수정

## 2. 제목 첫 글자는 소문자로, 끝에는 `.` 금지

## 3. 제목은 영문 기준 50자 이내로 할 것

## 4. 한 커밋에는 한 가지 문제만

추적 가능하게 유지한다. 너무 많은 문제를 한 커밋에 담으면 추적하기 어렵다.

## 예시

```bash
git commit -m "feat: 회원가입 기능 추가
본문 자세하게
"
```

## 자동 검사

`commitlint`가 위 규칙(커밋 유형, 제목 마침표, 제목 길이)을 `commit-msg` 훅에서 자동으로 검사한다. 규칙을 어기면 커밋 자체가 거부된다.

> 로컬 훅은 `git commit --no-verify`로 우회할 수 있다. 우회를 막으려면 GitHub 브랜치 보호 규칙 + CI에서의 검사가 추가로 필요하다.
