# FleaFlea 디자인 시스템 — 픽셀 파스텔 (Pixel-Pastel)

> 컨셉 한 줄 요약: **"픽셀 계단 모서리 + 파스텔 라벤더"** — 레트로 게임 감성을 UI 프레임 자체에 새기고, 색은 부드러운 파스텔로 눌러 귀엽되 정돈된 인상을 만든다.
> 랜딩/히어로 등 브랜드 임팩트가 필요한 순간에는 **우주풍 그라데이션 배경**(진한 보라 계열)을 더해 몰입감을 준다.
> 컬러·타이포 토큰은 `src/app/styles/tokens.css`에서 관리한다. 이 문서는 색 값을 복제하지 않고 **구조·규칙**만 다룬다.

---

## 1. 컨셉 원칙

1. **픽셀은 모서리로 표현한다.**
   카드·버튼·입력칸·칩의 모서리를 계단 모양으로 깎아 픽셀 감을 낸다. 도트 그림을 화면에 깔지 않고, **형태의 윤곽**만 픽셀로 만든다. 헬퍼는 `src/shared/lib/pixel.ts`:
   - `pixelBox(step)` — 네 모서리 (버튼·카드·칩). 작은 요소는 `2`, 버튼은 `4`, 큰 카드는 `6`
   - `pixelCorners('top' | 'bottom', step)` — 한쪽 변만 (차양, 선반)
   예외적으로 검색 입력창은 둥근 모서리(`rounded-full`)를 쓴다.

2. **마스코트는 한 캐릭터의 표정 변형으로만 늘린다.**
   `src/shared/config/mascots.ts`에 7종이 등록돼 있고 전부 같은 캐릭터의 표정·소품 변형이다. **새로운 캐릭터를 추가하지 않는다.**
   - `default` 기본 · `smile` `wink` 환영 · `surprised` 오류·확인 · `star` 장식 · `basket` 빈 상태 · `beret`
   - 마켓 커버가 없을 때는 `pickMascot(marketId)`로 마켓마다 고정된 마스코트를 고른다

3. **파스텔은 채도보다 톤으로 구분한다.**
   라벤더(`--color-primary` 계열)가 메인. 핑크·민트·블루는 **칩이나 픽셀 스프라이트 안에서만** 제한적으로 쓰고, 본문 카드 배경에 여러 파스텔을 동시에 깔지 않는다.

4. **상태는 색을 죽여서 표현한다.**
   거래가 끝났거나 예약된 상품은 사진을 블러 처리하고 어두운 오버레이 위에 상태 문구를 올린다. 비공개 도감 아이템은 무채색으로 완전히 죽인다.

5. **그라데이션은 "면"에만, 글자 뒤에는 쓰지 않는다.**
   `--gradient-dreamy`(핑크→라벤더→하늘)는 커버 자리, 히어로 배경, 카드 겉면처럼 **넓은 면**에 쓴다. 그 위에 글을 얹을 때는 흰 면을 한 겹 깔고 그 안에 쓴다.

---

## 2. 타이포그래피

폰트와 크기는 `tokens.css`에 정의돼 있다.

| 역할 | 토큰 | 크기 |
|---|---|---|
| 로고·브랜드 | `font-jua` (BM JUA) | 상황에 맞게 |
| 그 외 전체 | `font-sans` (Moneygraphy Pixel) | 아래 스케일 |

| 용도 | 클래스 | 크기 |
|---|---|---|
| 히어로 헤드라인 | `text-head-00` | 44px |
| 페이지 제목 | `text-head-02` | 28px |
| 섹션 제목·모달 제목 | `text-head-03` | 24px |
| 강조 본문 | `text-body-02` | 18px |
| 본문 | `text-body-03` | 16px |
| 보조·캡션 | `text-body-04` | 14px |
| 작은 라벨·칩 | `text-xs` / `text-[11px]` | 12 / 11px |

**규칙**
- `font-jua`는 로고에만 쓴다. 본문·버튼 라벨에 쓰지 않는다.
- 사용자가 입력한 값(닉네임, 상품명)은 **제목 자리에 넣지 않는다.** 길이를 통제할 수 없어 줄바꿈이 깨진다 → 7장 참고.

---

## 3. 픽셀 그래픽 규칙

- **마스코트 이미지**: `public/mascot/`. 항상 `[image-rendering:pixelated]`를 붙여 흐려지지 않게 한다.
- **스프라이트**: 문자열 그리드로 그리고 `<rect>`로 렌더한다 (`PixelShops.tsx` 참고). `shapeRendering="crispEdges"` 필수.
- **외곽선**: 스프라이트는 1px 진한 남색(`--navy-900`) 외곽선으로 감싼다.
- **채색**: 오브젝트당 2~4톤 평면 채색. 그라데이션 금지.
- **크기**: 정수배로만 확대한다. 임의 비율로 늘리면 계단이 뭉개진다.
- **표정은 상황과 일치시킨다.** 오류가 아닌데 놀란 표정, 로딩이 아닌데 로딩 모션을 쓰지 않는다.

---

## 4. 컴포넌트 패턴

### 픽셀 테두리 2겹 (기본형)
바깥에 연보라 판(`bg-primary-tint p-[2px]`), 안쪽에 흰 면. 둘 다 같은 `pixelBox` 값을 준다. 상품 카드·친구 줄이 이 구조다.

### 상품 카드
- 픽셀 테두리 2겹 + 4:3 이미지 칸
- 거래 유형 칩(판매·나눔·대여) → 제목 → 가격
- 거래중·완료면 사진 블러 + 어두운 오버레이에 상태 문구
- 카드 전체가 링크

### 폴라로이드 (상품·도감 물건 상세)

두 상세 화면은 `shared/ui/polaroid`의 `PolaroidPhoto`를 함께 사용한다. 사진 영역은 최대 360px로 맞추고 좁은 화면에서는 가용 폭에 맞춰 줄인다.
사진을 흰 액자로 감싸고 **아래쪽 여백만 두껍게** 둔다. 제목은 넣지 않고(옆 정보 칸과 중복) 그 자리 오른쪽에 마스코트를 작게 둔다. 마스킹 테이프를 액자 바깥에 곁들인다.

- 구조는 세 겹: 바깥 연보라 판(`pixelBox(4)` + `p-[2px]`) → 흰 액자(`pixelBox(4)`) → 사진 칸(`pixelBox(2)`)
- **흰 액자만으로는 윤곽이 생기지 않는다.** 페이지 배경이 흰색(`--color-bg`=`#fff`)이라 흰 면이 배경에 묻힌다. 바깥 연보라 판이 테두리 역할을 한다
- 그림자는 **`drop-shadow`(필터)로 준다.** `clip-path`는 `box-shadow`까지 잘라내 그림자가 사라진다. 필터는 잘린 계단 실루엣을 따라간다
- **액자를 회전시키지 않는다.** `clip-path` 계단은 회전하면 안티앨리어싱이 껴 픽셀 느낌이 죽는다. 비스듬한 인상이 필요하면 `clip-path`가 없는 요소(마스킹 테이프)를 기울인다
- **마스킹 테이프는 액자 바깥 형제 요소로 둔다.** `clip-path`는 자식도 함께 자르기 때문에, 액자 안에 넣으면 삐져나온 부분이 잘려 보이지 않는다. 테이프는 `shared/ui/doodle`
- 목록 카드에는 쓰지 않는다 (목록은 픽셀 테두리 2겹 유지)

### 말풍선 (상품 소개)
판매자 줄 바로 아래에 두어 "판매자가 하는 말"처럼 읽히게 한다. 꼬리는 위를 향한다.

- 본체는 `pixelBox(4)` + `bg-primary-subtle`, 꼬리는 `shapeRendering="crispEdges"` 계단 삼각형
- **꼬리도 본체의 `clip-path` 바깥 형제로 둔다.** 색은 본체와 같은 토큰(`--color-primary-subtle`)을 참조해 어긋나지 않게 한다
- 그림자는 본체가 아니라 **바깥 래퍼**에 건다 — 본체에만 걸면 꼬리에 그림자가 없어 따로 논다

### 마켓 카드 (목록)
- **차양**(줄무늬 + 물결)이 지붕처럼 얹힌 가게 형태. 차양은 **목록 카드 전용**이며 상세 페이지에는 쓰지 않는다
- 커버 · 제목 · 소개글 · `입장하기`
- 내가 만든 마켓에는 `HOST` 배지, 참여 중인 마켓에는 호스트 닉네임

### 모달
공용 컴포넌트 `shared/ui/modal`. ESC·바깥 클릭·X로 닫힌다.

| 크기 | 폭 | 쓰는 곳 |
|---|---|---|
| `lg` | 760px | 넓은 고르기 그리드 (상품 등록의 도감 물건 선택) |
| `md` (기본) | 600px | 폼 (마켓 만들기, 초대 코드, 초대 링크), 도감 교환 요청 |
| `compact` | 480px | 도감 대여·구걸 요청 (내부 여백 24px, 640px 이상 화면에서는 32px) |
| `sm` | 380px | 프로필·확인 (참여자, 친구 삭제) |

- 버튼은 **주요 동작을 왼쪽에**, 보조(닫기·취소)를 오른쪽에 나란히 둔다
- 내용 안에 `닫기` 버튼이 있으면 오른쪽 위 X는 `showClose={false}`로 끈다. 단 **확인 모달의 `취소`는 "하지 않겠다"는 답**이므로 X와 중복이 아니다 — 둘 다 둔다

### 빈 상태
마스코트 + 굵은 한 줄 + 보조 한 줄 + (필요하면) 다음 행동 버튼.

---

## 5. 인터럽트 · 흐름

사용자의 신뢰를 깨는 순간을 만들지 않기 위한 규칙. 스타일과 무관하게 전 화면에 예외 없이 적용한다.

- 진입 직후 전면 모달/바텀시트 없음. 시스템 권한 요청도 해당 기능 첫 사용 시점으로 미룬다.
- 이탈을 붙잡는 레이어를 두지 않는다. **단, 작성 중인 내용이 사라지는 경우만 예외**로 확인을 묻는다 (마켓 만들기 모달).
- 모든 모달·토스트에 명확한 닫기 경로가 있다. 회색으로 숨긴 거절 버튼도 위반으로 본다.
- 사용자가 요청한 동작 중간에 광고나 프로모션을 끼워넣지 않는다.
- CTA 위에 화면에서 이미 말한 내용을 반복하지 않는다.
- 브라우저 기본 `confirm`을 쓰지 않는다. 확인은 우리 모달로 한다.

---

## 6. 그래픽 사용 원칙

- 한 화면의 **핵심 그래픽(큰 마스코트 일러스트)은 하나**로 절제한다. 안내 박스의 작은 마스코트처럼 아이콘 크기(24~40px)는 별도로 센다.
- 디테일이 많은 그래픽은 충분히 크게, 단순한 아이콘은 작게.
- 그래픽이 핵심 정보나 CTA를 화면 아래로 밀어내지 않게 배치한다.
- 파티클, 의미 없는 장식은 넣지 않는다.
- 저해상도 이미지를 쓰지 않는다. 마스코트는 픽셀 렌더링을 켜서 선명하게 유지한다.

### 거절된 방향 (다시 제안하지 않음)
- **창살(십자 격자)** — 커버 이미지 위에 넣었다가 제거
- **나무 질감** — 라벤더 파스텔과 안 맞음
- **가랜드(삼각 깃발 줄)** — 사진 위에 얹으니 붙여놓은 티가 남
- **가판대 골조(기둥·좌판)** — 동일
- **커버 히어로 배너** — 큰 사진일 뿐 "가게" 신호가 없음
- **액자 3겹 프레임** — 가게가 아니라 사진 액자로 읽힘
- `PixelShops`를 상세 배경으로 — 스프라이트 안에 이미 차양이 있어 중복

> 배운 것: **큰 사진 위에 장식을 얹는 방식은 통하지 않는다.** 차양이 통했던 건 장식이어서가 아니라 카드의 **구조 자체**였기 때문이다.

---

## 7. 레이아웃 · 텍스트

### 페이지 골격
```
<Header />
<div className="mx-auto w-full max-w-7xl px-6 py-8 lg:px-8">
```
큰 화면에서 가로로 늘어지지 않도록 **최대 폭 + 반응형 여백**을 쓴다.

### 간격
Tailwind 기본 스케일(4px 단위)을 쓴다. 섹션 사이 `mt-10`~`mt-14`, 카드 안쪽 `p-4`~`p-6`, 목록 간격 `gap-3`.

### 한글 줄바꿈
전역 규칙은 `index.css`에 있다.
```css
body { word-break: keep-all; overflow-wrap: break-word; text-wrap: pretty; }
```
`word-break: keep-all`로 **한글 단어를 붙여 둔다.** 글자 단위로 끊으면 "프로필 수 / 정"처럼
단어 한가운데가 갈라져 읽기 나쁘다. 줄 끝이 들쑥날쑥해지는 것은 `text-wrap: pretty`로 완화하고,
단어 하나가 칸보다 길 때만 `overflow-wrap`이 끊는다.
이메일·초대 링크처럼 단어 안에서 끊어야 하는 값은 그 요소에서 `break-all`로 되돌린다.

### 사용자 입력 텍스트
- 길이를 통제할 수 없으므로 **제목 자리에 넣지 않는다.** 닉네임 20자, 마켓 소개 1000자, 상품 설명 2000자까지 올 수 있다.
- 긴 글은 **줄길이 제한(`max-w-*`) + 줄 수 접기 + 더보기**로 처리한다.
- 접는 줄 수는 **옆에 놓인 요소(커버 등)의 높이에 맞춘다.** 더 적게 접으면 옆에 빈 공간만 생긴다.
- `line-clamp`는 잘렸는지 알려주지 않으므로, 더보기 버튼은 **실제 넘침을 재서** 넘칠 때만 보여준다.

### 커서
Tailwind v4는 버튼에 `cursor: pointer`를 넣지 않는다. 이 프로젝트는 **링크와 버튼 모두 화살표**로 통일했다 (`index.css`의 `@layer base`).

---

## 8. Do / Don't

| ✅ Do | ❌ Don't |
|---|---|
| 카드·버튼 모서리를 `pixelBox`로 깎기 | 화면 전체를 도트 그림으로 채우기 |
| 마스코트는 한 캐릭터의 표정 변형으로 | 새로운 캐릭터·일러스트 스타일 추가 |
| 그라데이션은 넓은 면에만 | 글자 바로 뒤에 그라데이션 깔기 |
| 거래 끝난 상품은 블러 + 상태 오버레이 | 끝난 상품을 그대로 노출 |
| 사용자 입력 텍스트는 본문 크기에 | 닉네임·상품명을 제목(24px+)에 넣기 |
| 긴 글은 줄길이 제한 + 더보기 | 전문을 그대로 쏟아내기 |
| 확인은 우리 모달로 | 브라우저 `confirm` 사용 |
| 모달 주요 동작은 왼쪽 | 화면마다 버튼 순서 다르게 |
| 큰 마스코트는 화면당 하나 | 일러스트 두 개 이상 나란히 |

---

## 9. 관리 방법

- **색·타이포 토큰**은 `src/app/styles/tokens.css`가 단일 출처다. 컴포넌트에 hex를 직접 쓰지 않는다. 다만 픽셀 스프라이트는 SVG 문자열 안에 색을 직접 넣는다(CSS 변수를 못 쓰는 자리).
- **픽셀 헬퍼**는 `src/shared/lib/pixel.ts`. 새로 계단 모서리가 필요하면 여기 함수를 쓴다.
- **Tailwind 클래스는 문자열로 온전히 적는다.** `line-clamp-${n}`처럼 조립하면 클래스가 생성되지 않아 스타일이 조용히 빠진다.
- 인터럽트·그래픽 원칙은 코드 리뷰 체크리스트로도 쓸 수 있다.
