# FLEE 디자인 시스템 — 픽셀 무드 (Pixel-Pastel)

> 참고 화면: 홈 대시보드 (원본 캐릭터 & 내 물품 도감)
> 컨셉 한 줄 요약: **"8비트 픽셀 캐릭터 + 파스텔 라벤더 SaaS 카드"** — 레트로 게임보이 감성을 자극하되, 실제 UI는 모던하고 정돈된 카드형 레이아웃 위에 얹는다.
> 랜딩/히어로 등 브랜드 임팩트가 필요한 순간에는 여기에 **우주풍 그라데이션 배경**(진한 보라 계열, 홀로그램/포인트 그라데이션)을 더해 몰입감을 준다 — 단, 카드·아이콘·본문 UI는 여전히 플랫한 파스텔을 유지한다 (자세한 예외 범위는 6장 참고).
> 컬러 토큰은 이 문서에서 관리하지 않음 — Figma MCP 연동으로 실시간 동기화. 우주 그라데이션 팔레트만 예외로 `tokens.css`에 직접 정의한다 (Figma 소스 없음). 이 문서는 색을 제외한 구조·규칙만 다룬다.

---

## 1. 컨셉 원칙

1. **픽셀은 캐릭터·아이콘에만, 레이아웃은 모던하게.**
   화면 전체를 도트로 만들지 않는다. 배경/카드/버튼은 매끈한 라운드 UI를 쓰고, 그 안에 들어가는 캐릭터·상품 아이콘·마스코트만 픽셀 그래픽으로 표현한다. 이 대비가 "귀여움"을 만든다.
2. **한 캐릭터, 한 세계관.**
   FLEE 로고의 햄스터(혹은 다람쥐) 마스코트가 모든 곳에 반복 등장한다 — 로고, 유저 아바타, 빈 상태 일러스트, 잠금 아이콘. 캐릭터 종류를 늘리지 말고 이 마스코트의 표정/포즈 변형으로 통일한다.
3. **파스텔은 채도보다 톤으로 구분한다.**
   보라(라벤더)를 메인으로 두고, 상품 카테고리별로 민트·핑크·블루 파스텔을 "칩(원형 배지)" 색으로만 제한적으로 사용한다. 카드/리스트 등 본문 UI의 배경에 여러 파스텔을 동시에 쓰지 않는다 (히어로 배경 그라데이션은 예외 — 4번 참고).
4. **잠금·비공개 상태는 그레이스케일로 명확히 죽인다.**
   비공개 물건은 컬러를 완전히 빼고 회색 톤 자물쇠 아이콘으로 표시해, 공개 아이템과 시각적으로 확실히 구분한다.
5. **우주 그라데이션은 "임팩트 배경" 전용, 콘텐츠 위에는 쓰지 않는다.**
   랜딩 히어로처럼 화면 전체 배경 하나에만 진한 보라 그라데이션/홀로그램을 허용한다. 그 위에 얹는 카드·버튼·아이콘은 여전히 플랫한 파스텔+화이트를 유지해서 가독성과 기존 톤을 지킨다.

---

## 2. 타이포그래피

| 역할 | 폰트 성격 | 굵기 | 예시 |
|---|---|---|---|
| 헤드라인 (인사말 등) | 둥글고 통통한 고딕 (예: Cafe24 Ssurround, BM Dohyeon, 여기어때 잘난체) | Bold~ExtraBold | "반갑습니다, 은지님!" |
| 섹션 타이틀 | 동일 폰트, 한 단계 낮은 크기 | SemiBold | "등록한 최신 상품" |
| 본문/라벨 | 정갈한 시스템 산세리프 (Pretendard, Noto Sans KR) | Regular~Medium | 상품명, 메뉴 |
| 가격/숫자 | 본문과 동일 폰트 | SemiBold | "2,000원" |
| 캡션(날짜, 보조정보) | 본문 폰트 | Regular, 작은 크기 | "9.14 – 9.21" |

**규칙**: 헤드라인 폰트는 인사말·빈 상태 메시지처럼 "감정을 전달하는 자리"에만 쓰고, 표/리스트/버튼 라벨에는 절대 쓰지 않는다 (가독성 저하 방지).

---

## 3. 픽셀 아이콘 규칙

이 시스템의 핵심 자산입니다. 실제 제작(일러스트레이터가 있다면) 또는 AI 생성 시 아래 스펙을 그대로 프롬프트/가이드로 사용하세요.

- **캔버스 그리드**: 16×16px 또는 24×24px 픽셀 그리드에서 제작 후 32~40px로 표시 (업스케일 시 nearest-neighbor, 절대 안티에일리어싱 금지).
- **외곽선**: 1px 두께의 진한 보라/남색 계열 외곽선으로 모든 오브젝트를 감싼다.
- **채색**: 오브젝트당 2~4톤 평면 채색 (그라디언트 금지). 하이라이트는 1픽셀 밝은 톤으로만 표현.
- **배지(칩) 처리**: 모든 아이콘은 원형 파스텔 배경(chip) 위에 올라간다. 배경 원 지름 : 아이콘 크기 비율 = 대략 1.6:1 (아이콘 주변 여백 확보).
- **카테고리 매핑**: 아이콘 자체보다 "배경 칩 색"으로 카테고리를 구분 (예: 의류=핑크, 전자기기=블루, 카메라/취미=민트). 도감이 커져도 색 규칙만 지키면 확장 가능.
- **잠금(비공개) 아이콘**: 컬러 없이 회색 자물쇠 픽셀 아이콘 + 연회색 칩. 컬러 아이콘 사이에 섞여도 즉시 "안 보이는 것"임을 인지할 수 있어야 함.
- **마스코트 표정 변형**: 기본(중립) / 환영(눈웃음, 헤더) / 알림(놀람, 벨 아이콘 옆) / 빈 상태(시무룩) 최소 4종을 세트로 제작. 애원·호소하는 표정은 만들지 않는다 (죄책감 유도형 다크패턴 방지).

---

## 4. 컴포넌트 패턴

### 상품 카드
- 흰 배경, radius 12~16px, 은은한 그림자
- 좌상단에 판매자 미니 아바타(픽셀) + 닉네임 + "우리 동네" 같은 서브라벨을 한 줄로
- 우측에 카테고리 픽셀 아이콘 (칩 배경)
- 하단: 가격(semibold) + "자세히 보기" 텍스트 버튼

### 활발한 친구 마켓 카드
- 카드 전체가 브랜드 primary 색 배경 + 흰 텍스트인 "강조형" 카드 (일반 상품 카드와 위계 차등)
- 중앙 상단에 원형 마스코트, 하단에 마켓명 · 기간 · CTA

### 내 물품 도감 그리드
- 8열 그리드, 정사각 슬롯
- 공개 아이템: 컬러 픽셀 아이콘 + 파스텔 칩
- 비공개 아이템: 회색 자물쇠로 전량 대체 (아이콘 종류 노출 안 함 — 프라이버시 원칙)
- 하단에 진행형 바(공개/비공개 비율)와 텍스트 요약 ("내 물품 32개 · 공개 20 · 비공개 12")

### 거래 현황 리스트
- 좌측 아이콘: 상태별 마스코트 표정 아이콘 (대기=물음표 든 마스코트, 진행중=악수 포즈 등)
- 상태 라벨(semibold) + 상세 설명(보조 텍스트) 2줄 구조

---

## 5. 인터럽트 · 흐름

사용자의 신뢰를 깨는 순간을 만들지 않기 위한 규칙. 스타일과 무관하게 전 화면에 예외 없이 적용한다.

- 진입 직후 전면 모달/바텀시트 없음. 알림 권한 등 시스템 권한 요청도 해당 기능 첫 사용 시점(예: 첫 거래 요청 완료 후)으로 미룬다.
- 뒤로 가기나 이탈 시점에 붙잡는 레이어("정말 나가시겠어요?" 류) 없음.
- 모든 모달/바텀시트/토스트에 명확한 닫기 경로 존재. 회색으로 숨긴 거절 버튼도 위반으로 간주.
- 사용자가 요청한 동작(거래 요청, 물건 등록 등) 중간에 광고나 프로모션을 끼워넣지 않는다.
- CTA 버튼 위에 화면에서 이미 말한 내용을 반복하는 보조 설명을 두지 않는다.

---

## 6. 그래픽 사용 원칙

픽셀 마스코트·아이콘을 화면에 배치할 때의 절제 규칙.

- 한 화면에 핵심 그래픽(마스코트 일러스트)은 하나만. 나머지는 아이콘 단위로 강등한다.
- 단순한 그래픽(아이콘)은 작게, 디테일이 많은 그래픽(빈 상태 마스코트 일러스트 등)은 충분히 크게 보여준다.
- 아이콘 크기는 24~40px 고정, 두 개 이상을 나란히 병렬 배치하지 않는다.
- 그래픽이 핵심 정보나 CTA를 화면 아래로 밀어내지 않도록 배치한다.
- 파티클, 과한 그라데이션, 의미 없는 장식적 묘사는 넣지 않는다 (픽셀 아이콘의 "평면 채색" 원칙과도 일치). **예외**: 랜딩 히어로 등 "우주 임팩트 배경"(1장 참고)은 화면 전체 배경 하나에만, 화면당 최대 1곳까지 허용. 카드/아이콘/버튼 위에는 그라데이션을 쓰지 않는다.
- 그래픽 표정·상태는 실제 상황과 반드시 일치시킨다 (오류 상황이 아닌데 놀란 표정, 로딩 중이 아닌데 로딩 모션 금지).
- 그래픽이 텍스트나 CTA보다 시각적으로 튀지 않도록 화면 전체 균형을 맞춘다.
- 항상 고화질로 제작. 저해상도나 잔효과가 많으면 전체 퀄리티가 낮아 보인다.

> 참고: "만화적·손그림 느낌 배제" 같은 스타일 지침은 채택하지 않는다. FLEE는 픽셀 마스코트가 브랜드 정체성이므로, 위 항목은 어디까지나 배치·절제 규칙으로만 가져온다.

---

## 7. 스페이싱 & 레이아웃 그리드

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --space-7: 48px;

  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-pill: 999px;
}
```

- 섹션 간 수직 간격: `--space-6`
- 카드 내부 패딩: `--space-4`
- 그리드 갭: `--space-3`

### 해상도 · 대응 전략
- 기기별로 레이아웃을 따로 짜지 않고, 기준 해상도 하나를 정한 뒤 스케일링으로 대응한다.
- 브레이크포인트는 3~4개로 제한하고, 그 사이는 유동(fluid) 레이아웃으로 채운다.
- 일반 UI 에셋은 1x + 2x만 제공. 3x는 픽셀 아이콘처럼 품질이 특히 중요한 경우에만 추가한다.
- 픽셀 아이콘은 정수배(1x, 2x, 4x)로만 스케일 — 일반 이미지처럼 임의 비율로 늘리면 흐려진다.
- 노치, 카메라 홀, Dynamic Island는 Safe Area로 처리.
- 테스트는 대표 3~5종: 비율 다른 기기 2~3종, Safe Area 큰 기기 1종, 작은 화면 1종.

---

## 8. Do / Don't

| ✅ Do | ❌ Don't |
|---|---|
| 픽셀 요소는 아이콘·마스코트 등 "작은 오브젝트"에만 적용 | 버튼, 카드 테두리 등 UI 프레임 자체를 픽셀 도트로 그리기 |
| 카테고리는 칩 색상으로만 구분 | 배경 전체에 여러 파스텔을 동시에 사용 |
| 마스코트 표정으로 상태(알림/빈 상태/환영) 전달 | 매번 새로운 캐릭터/일러스트 스타일 추가 |
| 잠금 아이템은 완전 무채색 처리 | 비공개 아이템에 흐릿한 컬러 미리보기 노출 (정보 유출) |
| 권한 요청은 실사용 시점까지 지연 | 진입 직후 전면 모달/권한 팝업 노출 |
| 모든 레이어에 명확한 닫기 경로 제공 | 거절 버튼을 회색으로 숨기거나 찾기 어렵게 배치 |
| 화면당 핵심 그래픽 1개로 절제 | 아이콘·일러스트를 두 개 이상 나란히 병렬 배치 |

---

## 9. 파일로 관리할 때 팁

- 색상 토큰은 Figma MCP를 통해 실시간으로 가져와 컴포넌트에 바인딩한다. 이 문서에는 하드코딩된 hex 값을 두지 않는다. 우주 그라데이션 배경 팔레트만 Figma 소스가 없어 예외로 `tokens.css`에 직접 정의한다.
- 픽셀 아이콘 SVG/PNG는 `assets/icons/pixel/`처럼 별도 폴더로 관리하고, 새 아이콘 추가 시 "3. 픽셀 아이콘 규칙" 스펙(그리드, 외곽선, 톤 수)을 체크리스트로 사용하세요.
- 인터럽트·흐름, 그래픽 사용 원칙은 코드 리뷰 체크리스트로도 활용 가능 — PR 템플릿에 이 문서 링크를 걸어두면 좋다.